{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Workbench shell window. Workbench's own commands (set_attention, confirm_close, cancel_close, shell_ready) are defined by this crate, not by a plugin, so they are not listed here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
