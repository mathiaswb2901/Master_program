# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Node / UI
node_modules/
ui/dist/

# Playwright E2E output (reports, traces, per-run artifacts)
ui/playwright-report/
ui/test-results/
ui/blob-report/
# Perf lane output — its own folders so a perf run never overwrites a journey run
ui/playwright-report-perf/
ui/perf-results/

# Tauri
desktop/src-tauri/target/
# Generated by tauri-build on every run (capability schemas, plugin manifests)
desktop/src-tauri/gen/

# Local config & secrets — never commit
.env
workbench.toml
*.local.json
# Per-user workspace data (shortcuts.md and anything else Workbench keeps here)
.workbench/

# Local agent tooling: the PR pipeline script and the worktrees lanes run in.
# Machine-specific by nature (absolute paths, a local gh location), so it is
# never committed — see the danger-zone rule in CLAUDE.md.
.claude/

# Staged standalone backend — the PyInstaller onedir the packaging step copies
# here for `tauri build` to bundle. A large release artifact, never committed
# (`packaging/build_backend.ps1`, `desktop/src-tauri/tauri.conf.json`). The
# folder itself is tracked (via .gitkeep) because tauri.conf.json declares it as
# a bundle resource and the build fails if the path does not exist.
/desktop/src-tauri/backend/*
!/desktop/src-tauri/backend/.gitkeep

# OS noise
Thumbs.db
.DS_Store
